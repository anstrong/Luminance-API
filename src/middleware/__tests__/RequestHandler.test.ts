describe('Request Handler', () => {
    it.todo('should log the request')

    it.todo('should call next')

    describe('when there is no uid in the header', () => {
        it.todo('should call next with an error and Bad Request response')

        describe('when the endpoint does not require a uid', () => {
            it.todo('should call next')
        })
    })
})
